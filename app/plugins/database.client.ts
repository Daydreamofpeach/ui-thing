// Client-side database plugin - database operations should only run on server
export default defineNuxtPlugin(() => {
  // Database operations are handled server-side only
  // This plugin is kept for potential future client-side database features
})
